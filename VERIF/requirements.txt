# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 Tenstorrent USA Inc
#
# Cocotb-based testbench dependencies for VERIF/.
#
# Pinned to versions known to work with Verilator 5.046+ and VCS 2023.x.
# Adjust if your simulator combination needs different versions.

cocotb>=2.0.1,<3.0
cocotbext-axi>=0.1.28
pytest>=9.0.3
pytest-xdist>=3.8.0

# CSR reset readback test parses csr/aou-core.rdl directly via the same
# parser the peakrdl-* tools use. Pinned to the major version we exercise
# in scripts/gen_collateral.sh.
systemrdl-compiler>=1.32.2
